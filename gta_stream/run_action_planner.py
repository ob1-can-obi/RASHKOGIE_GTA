"""
Run a trained action planner checkpoint in GTA.

This script will be rebuilt once the model and training pipeline are complete.
It needs a trained checkpoint, model pipeline, and tokenizer to function.
"""

raise NotImplementedError(
    "run_action_planner.py is a placeholder — "
    "rebuild after training pipeline is done"
)
