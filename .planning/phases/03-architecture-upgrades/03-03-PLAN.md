---
phase: 03-architecture-upgrades
plan: 03
type: execute
wave: 2
depends_on:
  - 03-01
  - 03-02
files_modified:
  - tests/test_architecture.py
autonomous: true
requirements:
  - ARCH-01
  - ARCH-02
  - ARCH-03
  - ARCH-04

must_haves:
  truths:
    - "All ARCH-01 through ARCH-04 requirements have passing automated tests"
    - "MetaMLP structure test validates 3 hidden layers, skip connection, and LayerNorm"
    - "Encoder test validates 2 attention blocks, LayerNorm, residual, and output shape"
    - "Action planner test validates 2 hidden layers and correct dimensions"
    - "META_INPUT_DIM constant test validates the value equals 237"
    - "Full checkpoint roundtrip test validates save/load for all upgraded modules"
    - "Full test suite (all phases) passes with no regressions"
  artifacts:
    - path: "tests/test_architecture.py"
      provides: "Validation tests for ARCH-01, ARCH-02, ARCH-03, ARCH-04"
      min_lines: 80
  key_links:
    - from: "tests/test_architecture.py"
      to: "metacontroller/metacontroller.py"
      via: "import MetaMLP, META_INPUT_DIM"
      pattern: "from metacontroller import MetaMLP"
    - from: "tests/test_architecture.py"
      to: "main_model/main_model.py"
      via: "import create_encoder_weights, encode_state"
      pattern: "from main_model import create_encoder_weights"
    - from: "tests/test_architecture.py"
      to: "action_planner/action_planner.py"
      via: "import action_planner"
      pattern: "from action_planner import action_planner"
---

<objective>
Create the comprehensive validation test suite for all Phase 3 architecture upgrades. Tests cover MetaMLP structure and behavior (ARCH-01), encoder 2-block attention (ARCH-02), action planner 2-layer MLP (ARCH-03), META_INPUT_DIM constant (ARCH-04), and full checkpoint roundtrip for all upgraded modules.

Purpose: Wave 0 requirement from VALIDATION.md. Without these tests, the architecture upgrades cannot be verified automatically. The tests follow the project's established test patterns from test_batch_training.py.

Output: tests/test_architecture.py with 8 test functions covering all ARCH-* requirements.
</objective>

<execution_context>
@.claude/get-shit-done/workflows/execute-plan.md
@.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-architecture-upgrades/03-RESEARCH.md
@.planning/phases/03-architecture-upgrades/03-VALIDATION.md

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->

From metacontroller/metacontroller.py (after Plan 01):
```python
META_INPUT_DIM = 237

class MetaMLP(nn.Module):
    def __init__(self, input_dim=META_INPUT_DIM, output_dim=4):
        # layer1: Linear(input_dim, 256), ln1: LayerNorm(256)
        # layer2: Linear(256, 256), ln2: LayerNorm(256), skip_proj: Linear(input_dim, 256)
        # layer3: Linear(256, 128), ln3: LayerNorm(128)
        # out: Linear(128, output_dim)
    def forward(self, x):
        # h1 = relu(ln1(layer1(x)))
        # h2 = relu(ln2(layer2(h1) + skip_proj(x)))
        # h3 = relu(ln3(layer3(h2)))
        # return out(h3)
```

From main_model/main_model.py (after Plan 02):
```python
def create_encoder_weights(embed_dim=64, hidden_dim=128, fused_dim=128, num_heads=4):
    # Returns dict with: ego_mlp, scene_mlp, route_mlp, entity_mlp, fusion_mlp,
    #   qw1, kw1, vw1, ow1, ln_attn1,
    #   qw2, kw2, vw2, ow2, ln_attn2,
    #   embed_dim, num_heads

def encode_state(raw_state, weights):
    # Returns z_t [1, 128]
```

From action_planner/action_planner.py (after Plan 02):
```python
def action_planner(z_t, z_next_pred, vocab_size, planner_mlp=None, hidden_dim=128, top_k=3):
    # planner_mlp lazy init: Linear(256,256)->ReLU->Linear(256,128)->ReLU->Linear(128,V)
    # Returns dict with logits, token_probs, top_k_ids, top_k_probs, planner_mlp
```

Test conventions (from tests/test_batch_training.py):
```python
# Docstring starts with requirement ID: "ARCH-01: ..."
# Assertions include f-string error messages
# input_dim=10 for test-size MLPs
# torch.load uses weights_only=True, map_location="cpu"
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create test_architecture.py with all ARCH-* validation tests</name>
  <files>tests/test_architecture.py</files>
  <read_first>
    - tests/test_batch_training.py (first 50 lines for boilerplate and conventions)
    - tests/conftest.py (fixtures available)
    - metacontroller/metacontroller.py (to confirm MetaMLP class and META_INPUT_DIM after Plan 01)
    - main_model/main_model.py (to confirm encoder weights dict after Plan 02)
    - action_planner/action_planner.py (to confirm planner MLP after Plan 02)
    - .planning/phases/03-architecture-upgrades/03-VALIDATION.md (test map)
    - .planning/phases/03-architecture-upgrades/03-RESEARCH.md (Checkpoint Save/Load Compatibility Test example)
  </read_first>
  <action>
Create `tests/test_architecture.py` with the following structure and tests:

**File boilerplate** (follow test_batch_training.py pattern):
```python
"""
Tests for Phase 3: Architecture Upgrades (ARCH-01 through ARCH-04).

Each test validates one requirement from REQUIREMENTS.md.
Tests use synthetic data -- they do not require GTA or real game states.
"""

import sys
import os
import tempfile
from pathlib import Path

import torch
from torch import nn

PROJECT_ROOT = Path(__file__).resolve().parent.parent
METACONTROLLER_DIR = PROJECT_ROOT / "metacontroller"
MAIN_MODEL_DIR = PROJECT_ROOT / "main_model"
ACTION_PLANNER_DIR = PROJECT_ROOT / "action_planner"
TESTS_DIR = PROJECT_ROOT / "tests"

for d in (METACONTROLLER_DIR, MAIN_MODEL_DIR, ACTION_PLANNER_DIR, TESTS_DIR):
    if str(d) not in sys.path:
        sys.path.insert(0, str(d))

from metacontroller import MetaMLP, META_INPUT_DIM
from main_model import create_encoder_weights, encode_state
from action_planner import action_planner
```

**Test 1: `test_meta_mlp_structure`** (ARCH-01)
```python
def test_meta_mlp_structure():
    """ARCH-01: MetaMLP has 3 hidden layers (256-256-128), skip connection, LayerNorm."""
    m = MetaMLP()
    # Check layer dimensions
    assert m.layer1.in_features == 237 and m.layer1.out_features == 256, (
        f"Layer 1 should be (237, 256), got ({m.layer1.in_features}, {m.layer1.out_features})"
    )
    assert m.layer2.in_features == 256 and m.layer2.out_features == 256, (
        f"Layer 2 should be (256, 256), got ({m.layer2.in_features}, {m.layer2.out_features})"
    )
    assert m.layer3.in_features == 256 and m.layer3.out_features == 128, (
        f"Layer 3 should be (256, 128), got ({m.layer3.in_features}, {m.layer3.out_features})"
    )
    assert m.out.in_features == 128 and m.out.out_features == 4, (
        f"Output should be (128, 4), got ({m.out.in_features}, {m.out.out_features})"
    )
    # Check skip connection projection exists
    assert hasattr(m, 'skip_proj'), "Missing skip_proj for skip connection"
    assert m.skip_proj.in_features == 237 and m.skip_proj.out_features == 256, (
        f"skip_proj should be (237, 256), got ({m.skip_proj.in_features}, {m.skip_proj.out_features})"
    )
    # Check LayerNorm at each hidden layer
    assert hasattr(m, 'ln1') and isinstance(m.ln1, nn.LayerNorm), "Missing ln1"
    assert hasattr(m, 'ln2') and isinstance(m.ln2, nn.LayerNorm), "Missing ln2"
    assert hasattr(m, 'ln3') and isinstance(m.ln3, nn.LayerNorm), "Missing ln3"
    assert m.ln1.normalized_shape == (256,), f"ln1 shape: {m.ln1.normalized_shape}"
    assert m.ln2.normalized_shape == (256,), f"ln2 shape: {m.ln2.normalized_shape}"
    assert m.ln3.normalized_shape == (128,), f"ln3 shape: {m.ln3.normalized_shape}"
```

**Test 2: `test_meta_mlp_forward`** (ARCH-01)
```python
def test_meta_mlp_forward():
    """ARCH-01: MetaMLP forward pass produces [batch, 4] from [batch, 237] input."""
    m = MetaMLP()
    x = torch.randn(1, META_INPUT_DIM)
    out = m(x)
    assert out.shape == (1, 4), f"Expected (1, 4), got {out.shape}"

    # Test with batch > 1
    x_batch = torch.randn(4, META_INPUT_DIM)
    out_batch = m(x_batch)
    assert out_batch.shape == (4, 4), f"Expected (4, 4), got {out_batch.shape}"

    # Test with custom input_dim
    m_small = MetaMLP(input_dim=10)
    x_small = torch.randn(1, 10)
    out_small = m_small(x_small)
    assert out_small.shape == (1, 4), f"Expected (1, 4), got {out_small.shape}"
```

**Test 3: `test_meta_mlp_save_load`** (ARCH-01)
```python
def test_meta_mlp_save_load():
    """ARCH-01: MetaMLP save/load roundtrip produces identical output."""
    m = MetaMLP()
    x = torch.randn(1, META_INPUT_DIM)
    out_before = m(x)

    with tempfile.TemporaryDirectory() as tmpdir:
        path = os.path.join(tmpdir, "meta_mlp.pt")
        torch.save({"model_state_dict": m.state_dict()}, path)

        m2 = MetaMLP()
        ckpt = torch.load(path, map_location="cpu", weights_only=True)
        m2.load_state_dict(ckpt["model_state_dict"])

        out_after = m2(x)
        assert torch.allclose(out_before, out_after), (
            f"Save/load roundtrip mismatch: max diff = {(out_before - out_after).abs().max().item()}"
        )
```

**Test 4: `test_meta_input_dim_constant`** (ARCH-04)
```python
def test_meta_input_dim_constant():
    """ARCH-04: META_INPUT_DIM constant equals 237."""
    assert META_INPUT_DIM == 237, f"META_INPUT_DIM should be 237, got {META_INPUT_DIM}"
    # Verify the breakdown: fused_dim(128) + scalars(10) + top_k(3) + top_k*embed(96) = 237
    expected = 128 + 10 + 3 + 96
    assert META_INPUT_DIM == expected, (
        f"META_INPUT_DIM={META_INPUT_DIM} doesn't match breakdown sum={expected}"
    )
```

**Test 5: `test_encoder_two_attention_blocks`** (ARCH-02)
```python
def test_encoder_two_attention_blocks():
    """ARCH-02: Encoder uses 2 attention blocks with LayerNorm."""
    w = create_encoder_weights()

    # Block 1 weights must exist
    for key in ("qw1", "kw1", "vw1", "ow1"):
        assert key in w, f"Missing block 1 weight: {key}"

    # Block 2 weights must exist
    for key in ("qw2", "kw2", "vw2", "ow2"):
        assert key in w, f"Missing block 2 weight: {key}"

    # LayerNorm for each block
    assert "ln_attn1" in w and isinstance(w["ln_attn1"], nn.LayerNorm), (
        "Missing or wrong type: ln_attn1"
    )
    assert "ln_attn2" in w and isinstance(w["ln_attn2"], nn.LayerNorm), (
        "Missing or wrong type: ln_attn2"
    )

    # Block 1 query dim = embed_dim * 3 = 192
    assert w["qw1"].shape[1] == 192, (
        f"Block 1 query dim should be 192, got {w['qw1'].shape[1]}"
    )

    # Block 2 query dim = embed_dim = 64
    assert w["qw2"].shape[1] == 64, (
        f"Block 2 query dim should be 64, got {w['qw2'].shape[1]}"
    )

    # Old keys must NOT exist
    for old_key in ("qw", "kw", "vw", "ow"):
        assert old_key not in w, f"Old key still present: {old_key}"
```

**Test 6: `test_encoder_output_shape`** (ARCH-02)
```python
def test_encoder_output_shape():
    """ARCH-02: Encoder output shape unchanged at [1, 128] after upgrade."""
    w = create_encoder_weights()
    raw_state = {
        "near_entities": [],
        "near_vehs": [],
        "near_peds": [],
        "near_objects": [],
    }
    z_t = encode_state(raw_state, w)
    assert z_t.shape == (1, 128), f"z_t shape should be (1, 128), got {z_t.shape}"
```

**Test 7: `test_planner_two_layers`** (ARCH-03)
```python
def test_planner_two_layers():
    """ARCH-03: Action planner MLP has 2 hidden layers (256, 128)."""
    z_t = torch.randn(1, 128)
    z_next = torch.randn(1, 128)
    result = action_planner(z_t, z_next, vocab_size=874)

    mlp = result["planner_mlp"]
    linear_layers = [m for m in mlp if isinstance(m, nn.Linear)]

    assert len(linear_layers) == 3, (
        f"Expected 3 Linear layers (2 hidden + output), got {len(linear_layers)}"
    )
    assert linear_layers[0].in_features == 256 and linear_layers[0].out_features == 256, (
        f"Layer 0 should be (256, 256), got ({linear_layers[0].in_features}, {linear_layers[0].out_features})"
    )
    assert linear_layers[1].in_features == 256 and linear_layers[1].out_features == 128, (
        f"Layer 1 should be (256, 128), got ({linear_layers[1].in_features}, {linear_layers[1].out_features})"
    )
    assert linear_layers[2].in_features == 128 and linear_layers[2].out_features == 874, (
        f"Layer 2 should be (128, 874), got ({linear_layers[2].in_features}, {linear_layers[2].out_features})"
    )
```

**Test 8: `test_full_checkpoint_roundtrip`** (ARCH-04 success criterion)
```python
def test_full_checkpoint_roundtrip():
    """ARCH-04: All modules serialize to disk and reload cleanly."""
    # MetaMLP roundtrip
    meta = MetaMLP()
    x_meta = torch.randn(1, META_INPUT_DIM)
    out_meta_before = meta(x_meta)

    # Encoder weights roundtrip (save individual components)
    enc_w = create_encoder_weights()
    raw_state = {"near_entities": [], "near_vehs": [], "near_peds": [], "near_objects": []}
    z_before = encode_state(raw_state, enc_w)

    # Action planner roundtrip
    z_t = torch.randn(1, 128)
    z_next = torch.randn(1, 128)
    planner_result = action_planner(z_t, z_next, vocab_size=100)
    planner_mlp = planner_result["planner_mlp"]
    planner_out_before = planner_mlp(torch.cat([z_t, z_next], dim=-1))

    with tempfile.TemporaryDirectory() as tmpdir:
        # Save
        torch.save({"model_state_dict": meta.state_dict()}, os.path.join(tmpdir, "meta.pt"))
        torch.save({"model_state_dict": planner_mlp.state_dict()}, os.path.join(tmpdir, "planner.pt"))

        # Reload MetaMLP
        meta2 = MetaMLP()
        ckpt = torch.load(os.path.join(tmpdir, "meta.pt"), map_location="cpu", weights_only=True)
        meta2.load_state_dict(ckpt["model_state_dict"])
        out_meta_after = meta2(x_meta)
        assert torch.allclose(out_meta_before, out_meta_after), "MetaMLP roundtrip failed"

        # Reload planner
        planner2 = nn.Sequential(
            nn.Linear(256, 256), nn.ReLU(),
            nn.Linear(256, 128), nn.ReLU(),
            nn.Linear(128, 100),
        )
        ckpt_p = torch.load(os.path.join(tmpdir, "planner.pt"), map_location="cpu", weights_only=True)
        planner2.load_state_dict(ckpt_p["model_state_dict"])
        planner_out_after = planner2(torch.cat([z_t, z_next], dim=-1))
        assert torch.allclose(planner_out_before, planner_out_after), "Planner roundtrip failed"
```
  </action>
  <verify>
    <automated>.venv/bin/python -m pytest tests/test_architecture.py -x -v</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "def test_" tests/test_architecture.py` returns 8
    - `grep -c "ARCH-01" tests/test_architecture.py` returns at least 3 (in test docstrings)
    - `grep -c "ARCH-02" tests/test_architecture.py` returns at least 2
    - `grep -c "ARCH-03" tests/test_architecture.py` returns at least 1
    - `grep -c "ARCH-04" tests/test_architecture.py` returns at least 2
    - `grep -c "weights_only=True" tests/test_architecture.py` returns at least 3
    - `.venv/bin/python -m pytest tests/test_architecture.py -x -v` shows 8 passed, 0 failed
  </acceptance_criteria>
  <done>tests/test_architecture.py exists with 8 test functions covering all ARCH-01 through ARCH-04 requirements. All tests pass. Full test suite (all phases) passes with no regressions.</done>
</task>

<task type="auto">
  <name>Task 2: Run full test suite to confirm no regressions</name>
  <files></files>
  <read_first>
    - tests/test_architecture.py (just created)
  </read_first>
  <action>
1. Run the full test suite across all test files:
   ```bash
   .venv/bin/python -m pytest tests/ -x -v
   ```

2. If any test fails, diagnose and fix. Common issues:
   - `test_batch_training.py` tests using `mock_meta_mlp` fixture: should now get MetaMLP(input_dim=10) instead of nn.Sequential. The MetaMLP is callable with the same interface (`mlp(features)`) so tests should pass. If any test inspects internal structure of the MLP (e.g., iterating Sequential layers), update that test to work with MetaMLP.
   - `test_training_correctness.py` (if it exists): same consideration.

3. Confirm all tests pass with zero failures.
  </action>
  <verify>
    <automated>.venv/bin/python -m pytest tests/ -x -v</automated>
  </verify>
  <acceptance_criteria>
    - `.venv/bin/python -m pytest tests/ -x -v` shows 0 failures
    - All test files run: test_architecture.py, test_batch_training.py, and any others present
  </acceptance_criteria>
  <done>Full test suite passes with 0 failures. No regressions from Phase 3 architecture upgrades.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none) | Tests only -- no new trust boundaries |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-03 | Tampering | torch.load in test roundtrips | accept | Tests use tempfile directories deleted after test. weights_only=True used on all load calls. |
</threat_model>

<verification>
1. `.venv/bin/python -m pytest tests/test_architecture.py -x -v` -- all 8 tests pass
2. `.venv/bin/python -m pytest tests/ -x -v` -- full suite passes (no regressions)
3. Each test includes descriptive assertion error messages
4. Tests follow project conventions (docstring with requirement ID, f-string messages)
</verification>

<success_criteria>
- 8 test functions in test_architecture.py
- All 8 pass
- Full test suite (all phases) passes with 0 failures
- Every ARCH-* requirement has at least one test
</success_criteria>

<output>
After completion, create `.planning/phases/03-architecture-upgrades/03-03-SUMMARY.md`
</output>
